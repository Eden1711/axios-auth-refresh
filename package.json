{
  "name": "axios-auth-refresh-queue",
  "version": "1.5.0",
  "description": "Bulletproof Axios Interceptor for JWT Refresh Token",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "prepublishOnly": "npm run test && npm run build",
    "test": "vitest run"
  },
  "keywords": [
    "axios",
    "auth",
    "jwt",
    "refresh-token",
    "interceptor"
  ],
  "author": "Tai Tran (Eden)",
  "license": "MIT",
  "peerDependencies": {
    "axios": ">=0.19.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "axios": "^1.13.2",
    "axios-mock-adapter": "^2.1.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}
